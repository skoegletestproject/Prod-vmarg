name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy-job:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Render
        run: curl -X POST "https://api.render.com/deploy/srv-culhsbt6l47c73dovrvg?key=uXhJVoU0ceU"
